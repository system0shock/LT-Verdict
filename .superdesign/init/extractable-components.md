# Extractable components

No application components are available for extraction.

The static header, KPI boxes, table, and footer in
`docs/exec-summary-lt-verdict.html` are print-document patterns rather than
shared application components. Superdesign should create the Slice 1 dashboard
without extracting them as `DraftComponent` entities.
