# UI components

The repository has no application frontend, JavaScript package manifest, UI
framework, or shared component library yet.

`docs/exec-summary-lt-verdict.html` is a standalone printable project summary,
not an application component source. It contains no reusable component code.
